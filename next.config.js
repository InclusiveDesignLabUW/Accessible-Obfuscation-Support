/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    images: {
        unoptimized: true,
        assetPrefix: '.',
      },
  }
   
  module.exports = nextConfig